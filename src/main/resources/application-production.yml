server:
  port: 8081
  ssl:
    enabled: true
    certificate: "classpath:keys/fullchain1.pem"
    certificate-private-key: "classpath:keys/privkey1.pem"
spring:
  application:
    name: moup-api
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://moup.chaau6g6cyaz.us-east-2.rds.amazonaws.com:3306/moup
    username: admin
    password: Moup123!

app:
  environment:
    production: true
  cors:
    allowed-origins: https://moup.io